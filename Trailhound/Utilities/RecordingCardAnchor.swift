import CoreGraphics

/// Global frame of the live recording card (for stop credits morph).
struct RecordingCardAnchor: Equatable {
    var minX: CGFloat = 0
    var minY: CGFloat = 0
    var width: CGFloat = 0
}
